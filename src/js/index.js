import Deck from './models/Deck';

/** Global state of the app
 * - Deck object
 * - Player object (hand)
 * - Dealer object (hand)
 */

const state = {};

const controlDeck = () => {
  
}
const deck = new Deck(6);
deck.getData(6);
